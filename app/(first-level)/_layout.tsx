import { View, Text } from 'react-native';
import React from 'react';
import { Slot } from 'expo-router';

type Props = {};

const FirstLevelLayout = (props: Props) => {
  return <Slot />;
};

export default FirstLevelLayout;
